# React + TypeScript + Vite + shadcn/ui

This is a template for a new Vite project with React, TypeScript, and shadcn/ui.

## Project setup

```bash
$ npm install
```

## Compile and run the project

```bash
npm run dev
```

DISCLAIMER: Expects running docker compose with BE and DB!

## Manual interaction

`http://localhost:5173`